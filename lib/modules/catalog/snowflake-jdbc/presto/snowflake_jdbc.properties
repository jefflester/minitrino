connector.name=snowflake-jdbc
connection-url=${ENV:SNOWFLAKE_JDBC_CONNECT_URL}
connection-user=${ENV:SNOWFLAKE_JDBC_CONNECT_USER}
connection-password=${ENV:SNOWFLAKE_JDBC_CONNECT_PASSWORD}
snowflake.warehouse=${ENV:SNOWFLAKE_JDBC_WAREHOUSE}
snowflake.database=${ENV:SNOWFLAKE_JDBC_DB}
