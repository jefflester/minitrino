connector.name=snowflake-distributed
connection-url=${ENV:SNOWFLAKE_DIST_CONNECT_URL}
connection-user=${ENV:SNOWFLAKE_DIST_CONNECT_USER}
connection-password=${ENV:SNOWFLAKE_DIST_CONNECT_PASSWORD}
snowflake.warehouse=${ENV:SNOWFLAKE_DIST_WAREHOUSE}
snowflake.database=${ENV:SNOWFLAKE_DIST_DB}
snowflake.stage-schema=${ENV:SNOWFLAKE_DIST_STAGE_SCHEMA}
allow-drop-table=true
