---
services:

  minitrino:
    labels:
      - org.minitrino=root
      - org.minitrino.module=catalog-test

  test:
    build:
      context: ./modules/catalog/test/
      labels:
        - org.minitrino=root
    command: [tail, -f, /dev/null]  # Keep alive
    image: minitrino/test:latest
    container_name: test-${CLUSTER_NAME}
    volumes:
      - test-data:/etc/test/
    labels:
      - org.minitrino=root
      - org.minitrino.module=catalog-test

volumes:
  test-data:
    labels:
      - org.minitrino=root
      - org.minitrino.module=catalog-test
