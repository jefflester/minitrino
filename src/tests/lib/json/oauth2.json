{
  "tests": [
    {
      "type": "shell",
      "name": "Ensure oauth2 server is accessible",
      "container": "minitrino-lib-test",
      "command": "echo 'pass'",
      "exitCode": 0,
      "healthcheck": {
        "command": "curl -s -k https://host.docker.internal:8100/minitrino-issuer/.well-known/openid-configuration",
        "container": "minitrino-lib-test",
        "retries": 30,
        "exitCode": 0,
        "contains": [
          "issuer",
          "authorization_endpoint",
          "end_session_endpoint"
        ]
      }
    },
    {
      "type": "logs",
      "name": "Ensure cluster is running",
      "container": "minitrino-lib-test",
      "contains": [
        "=== SERVER STARTED ==="
      ],
      "timeout": 60
    }
  ]
}
